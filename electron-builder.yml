appId: com.clintmaruti.sudoku
productName: Sudoku
asar: true
artifactName: ${productName}-${version}-${arch}.${ext}
directories:
  output: release
files:
  - out/**/*
  - dist/**/*
  - package.json
  - "!**/*.map"
mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  category: public.app-category.games
win:
  target:
    - target: nsis
      arch:
        - x64
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
